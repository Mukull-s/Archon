import { Router } from 'express';

const router = Router();

/**
 * Repository routes — Ingestion & analysis.
 * Will be implemented in Part 3.
 * 
 * POST /api/repos/analyze   → Ingest a GitHub repository
 * GET  /api/repos/:id       → Get analysis results
 */

export default router;
